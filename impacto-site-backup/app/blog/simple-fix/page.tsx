import React from 'react';
import SimpleFix from '../simple-fix';

export default function SimpleFixPage() {
  return <SimpleFix />;
}

export const metadata = {
  title: 'Simple Blog Fix | Impacto ONG',
  description: 'Fix blog system issues with a simplified approach',
}; 