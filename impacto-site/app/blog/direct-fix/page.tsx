import React from 'react';
import DirectFix from '../direct-fix';

export default function DirectFixPage() {
  return <DirectFix />;
}

export const metadata = {
  title: 'Blog Direct Fix | Impacto ONG',
  description: 'Fix blog system issues directly',
}; 